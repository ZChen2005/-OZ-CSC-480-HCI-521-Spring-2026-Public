<server description="auth server">
    <!-- tag::featureManager[] -->
    <featureManager>
        <platform>jakartaee-10.0</platform>
        <platform>microprofile-7.1</platform>
        <feature>beanValidation</feature>
        <!-- tag::cdiFeature[] -->
        <feature>cdi</feature>
        <!-- end::cdiFeature[] -->
        <feature>jsonb</feature>
        <!-- tag::passwordUtilFeature[] -->
        <feature>passwordUtilities-1.1</feature>
        <!-- end::passwordUtilFeature[] -->
        <feature>restfulWS</feature>
        <!-- tag::sslFeature[] -->
        <feature>ssl-1.0</feature>
        <!-- end::sslFeature[] -->
        <!-- tag::mpConfigFeature[] -->
        <feature>mpConfig</feature>
        <!-- end::mpConfigFeature[] -->
        <feature>mpOpenAPI</feature>
        <!-- for google oauth -->
        <feature>socialLogin-1.0</feature>
        <!-- to create JWT -->
        <feature>jwt-1.0</feature>
        <!-- to validate jwt i dont think if we need here but does not harm to have it  -->
        <feature>mpJwt</feature>
        <feature>appSecurity</feature>
    </featureManager>
    <!-- end::featureManager[] -->

    <variable name="http.port" defaultValue="9084"/>
    <variable name="https.port" defaultValue="9444"/>
    <variable name="app.context.root" defaultValue="/auth"/>

    <!-- tag::httpEndpoint[] -->
    <httpEndpoint
        host="*" 
        httpPort="${http.port}" 
        httpsPort="${https.port}" 
        id="defaultHttpEndpoint"
    />
    <!-- end::httpEndpoint[] -->

    <!-- tag::webApplication[] -->
    <webApplication 
        location="auth.war"
        contextRoot="${app.context.root}">
        <application-bnd>
        <security-role name='users'>
        <special-subject type="ALL_AUTHENTICATED_USERS"/>
        </security-role>
        </application-bnd>
    </webApplication>
    <!-- end::webApplication[] -->
    <!-- tag::sslContext[] -->
    <!-- tag::keyStore[] -->
    <keyStore
        id="outboundTrustStore" 
        location="${server.output.dir}/resources/security/truststore.p12"
        password="mongodb"
        type="PKCS12" 
    />
    <!-- end::keyStore[] -->
    <!-- tag::ssl[] -->
    <ssl 
        id="outboundSSLContext" 
        keyStoreRef="defaultKeyStore" 
        trustStoreRef="outboundTrustStore"
        sslProtocol="TLS"
    />
    <!-- end::ssl[] -->
    <!-- for google oauth  -->
    <!-- havent included secrets -->
    <googleLogin id="googleLogin" clientId = "" clientSecret="" scope="openid profile email"/>
    <!-- for jwt -->
    <jwtBuilder id="" issuer="" expiry="1h" audiences="worklog" signatureAlgorithm="RS256"/>
    <!-- keystore to sign JWT -->
    <keyStore id="defaultKeyStore" password="" location="${server.config.dir}/resources/security/key.p12" type="PKCS12"/>
    <!-- SSL FOR GOOGLE -->
    <ssl 
        id="defaultSSLConfig" 
        keyStoreRef="defaultKeyStore" 
        trustDefaultCerts="true"
    />
    <!-- end::sslContext[] -->
</server>
