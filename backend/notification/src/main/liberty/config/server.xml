<server description="Notification Microservice">
    <featureManager>
        <platform>jakartaee-10.0</platform>
        <platform>microprofile-7.1</platform>
        <feature>beanValidation</feature>
        <feature>cdi</feature>
        <feature>jsonb</feature>
        <feature>passwordUtilities-1.1</feature>
        <feature>restfulWS</feature>
        <feature>mpConfig</feature>
        <feature>mpOpenAPI</feature>
    </featureManager>

    <!-- cors allowed -->
    <cors domain="*"
        allowedOrigins="*"
        allowedMethods="GET, POST, PUT, DELETE, OPTIONS"
        allowedHeaders="Content-Type, Authorization"
        maxAge="3600" />

    <!-- tag::httpEndpoint[] -->
    <httpEndpoint
        host="*"
        httpPort="${http.port}"
        httpsPort="${https.port}"
        id="defaultHttpEndpoint"
    />
    <!-- end::httpEndpoint[] -->

    <!-- tag::webApplication[] -->
    <webApplication
        location="notifier.war"
        contextRoot="${app.context.root}"
    />
    <!-- end::webApplication[] -->


    <variable name="mongo.hostname" defaultValue="localhost" />
    <variable name="mongo.port" defaultValue="27017" />
    <variable name="mongo.dbname" defaultValue="appdb" />
    <variable name="mongo.username" defaultValue="appuser" />
    <variable name="mongo.password" defaultValue="secret123" />


</server>